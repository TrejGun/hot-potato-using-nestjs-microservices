"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const terminus_constants_1 = require("./terminus.constants");
const utils_1 = require("./utils");
/**
 * Create a wrapper so it is injectable & easier to test
 *
 * @internal
 */
exports.TerminusLibProvider = {
    provide: terminus_constants_1.TERMINUS_LIB,
    useFactory: () => {
        const [terminus] = utils_1.checkPackages(['@godaddy/terminus'], 'the legacy Terminus API');
        return terminus === null || terminus === void 0 ? void 0 : terminus.createTerminus;
    },
};
